# Lumin v2 Workboard

Status: Phase 1 foundation implementation active

Revision: 2026-08-01

## One-Line Purpose

Lumin is a native repository-audit engine and Codex/Claude Code skill that gives AI agents grounded, queryable evidence before and after they change code.

## Spec Registry

| ID | Status | Document | One-line role |
| --- | --- | --- | --- |
| METHOD-000 | active | `문서(한글)/SDD.ko.md` (canonical; `SDD.md` English translation) | Defines the permanent Spec-Driven Development workflow for every change. |
| PRODUCT-000 | frozen | `specs/000-product-contract.md` | Defines what Lumin v2 must guarantee and what it is not. |
| ARCH-000 | frozen | `architecture/000-system-blueprint.md` | Owns the final system shape, crate boundaries, and dependency direction. |
| ARCH-001 | frozen | `architecture/001-execution-and-ownership.md` | Owns the Kahn/Rayon execution model, determinism, and memory ownership. |
| ARCH-002 | frozen | `architecture/002-evidence-and-write-gate.md` | Owns the evidence store, bounded query protocol, and pre/post transaction. |
| SLICE-001 | active | `specs/001-foundation-slice.md` | Defines the JS/TS/SFC foundation, with Vue as the first production dialect. |
| REVIEW-001 | superseded | `reviews/architecture-v1-adversarial-2026-07-15.md` | Records the first adversarial findings and first amendment decisions. |
| REVIEW-002 | complete, monitoring | `reviews/architecture-v1-independent-verification-2026-07-15.md` | Preserves the exact independent verification and may reopen only on a concrete regression or counterexample. |

## Active Work

Implement the first production Rust vertical path from native repository admission through JS/TS inventory, OXC lowering, deterministic graph construction, canonical zero-fan-in findings, and machine output. The numeric targets under `reviews/probes/phase0-numeric-target-selection-2026-07-18/` are approved Phase 1 budgets, not achieved-product claims. Their external audit is non-blocking and may reopen the contract only with a concrete counterexample. Product packages, packaged skill adapters, public behavior, native path/root product round trips, and achieved-budget proofs remain Phase 1 acceptance.

Current implementation checkpoint:

- A real twelve-crate Rust path now owns repository inventory, OXC fact lowering, Vue SFC decomposition/finalization, package.json and restricted pnpm workspace facts, JSONC config admission, config-owned resolution profiles, relative plus `baseUrl`/`paths` resolution, profile-aware workspace package surfaces, deterministic graph reduction, canonical dead-export evidence, JSON protocol output, and persisted audit/overview/findings commands.
- `jobs=1` and multi-worker analysis produce identical semantic evidence in the checked behavior tests.
- Generated and vendored findings remain canonical `ReviewOnly` rows. Parse, duplicate package identity, unsupported config, and unresolved-input uncertainty remains visible as typed incomplete evidence rather than a muted clean result. Package-local public-surface uncertainty does not erase unrelated `ReviewOnly` findings.
- Resolver semantic inputs follow demand -> inventory capture -> resume. Relative `extends` uses exact then one `.json` candidate, invocation profiles replace only config profile selection, and selected profiles are persisted in run overview evidence.
- Restricted pnpm membership replaces same-directory `package.json#workspaces`, applies positive patterns before exclusions, and hard-stops malformed or unsupported YAML without silently falling back. Package resolution owns active-profile `exports`, public entry fallback, type/value lanes, and public-surface declarations; unsupported `imports`, targets, package fields, and importer formats remain typed and package-local.
- `lumin-sfc` now owns the dialect-neutral SFC entry point and the first Vue path. Inline scripts retain `EmbeddedSourceUnitId` and parent spans through engine-routed OXC extraction; exact external scripts attach existing logical facts; template component imports and relative style resources finalize under the parent Vue module. Malformed Vue, opaque template bindings, missing or mode-conflicting external scripts, and unavailable Svelte/Astro dialects remain typed evidence.
- The durable write-gate vertical now admits existing files, bounded directories, and new source paths from their nearest existing parent. Logical leases retain canonical paths plus observed real-directory prefix identities; directory scopes conflict with descendants; physical aliases expand into caller-visible logical leases and are reanalyzed separately; late unleased alias topology is denied.
- Successful closes append an immutable monotonic worktree transition and references from every earlier active gate in the same store transaction. A disjoint gate stays `Incomplete` while another writer is active, then reconciles that writer's exact terminal before/after chain on retry. Broken chains deny, protected-read transitions are stale, and only authorizing closes release leases and transition references.
- Existing grounded dead-export findings and bounded unresolved-edge facts now lower into model-owned `DeltaKey` facts and one deterministic total lifecycle relation over identity, target/domain sets, confidence, grounding, evidence identity, and owner payload. Introduced adverse facts deny, unchanged adverse facts warn, improvements/resolutions authorize without an adverse signal, and every classification is persisted on the gate revision and public operation/gate response. Unsupported or unbounded owner semantics remain typed required-evidence gaps; they are not fabricated into comparable facts until their exact target and affected domain can be represented.
- Pre-write and close-time resolver analysis now retain one owned extraction session across `NeedsInputs` steps. Each new config path and its observed physical identity are transactionally recorded on the pending operation before inventory captures it; active/provisional writers, including physical aliases, make the attempt typed `Incomplete`, and a later writer cannot cross a live reservation. Pre-write promotion rejects a baseline that omits or physically disagrees with any reserved demand. Successful closure persists the exact finished protected read set on the revision and gate. Until an exact actual-write set is implemented, a first-seen close-time input outside the lease remains a fail-closed `UnplannedWrite`; a failed attempt cannot launder it into an authorizing retry.
- Every pending pre-write or post-write operation now owns an operation-specific OS file lock for its full analysis lifetime. Process death releases that lock without a timeout; the next operation or `operation show` atomically marks the dead record `Interrupted`, increments its interruption count, clears only its provisional write/semantic-read reservations and liveness binding, and leaves the gate revision unchanged. The same operation ID and digest may then reacquire current reservations, while a live duplicate session is a typed busy failure and a different close cannot duplicate one live gate revision. Terminal commits clear operation-scoped reservations and liveness in the same transaction while preserving the active gate's durable leases and protected reads.
- `lumin gate abandon <gate-id> --operation-id <operation-id> --reason <text>` now binds its request digest to the exact active revision and reason. One store transaction appends the sole `Abandoned` revision, releases durable write leases, protected semantic inputs, alias closure, and transition references, and commits the recoverable operation result. An exact retry after process reopen returns the same result; conflicting reuse, stale revision, a live close, or any second terminal operation fails without another revision.
- Inventory now captures the canonical native root plus its high-resolution physical identity and derives the model-owned `RepositoryId`; every engine entry routes that one binding into the store. Canonical lock, marker, anchor, and lifecycle-store headers persist the repository/root binding together with the state-directory identity, immutable one-link lifecycle-lock identity, namespace nonce, and exact kind-ordered `attempts | runs | trash | cache` directory/anchor/parent-nonce set. Every store lock scope carries held root/state/parent/anchor handles, revalidates the complete binding before and after physical mutations and lifecycle commits, and hard-stops copied/replaced state directories, locks, parents, anchor replacement or extra links, redirects, foreign repository bindings, and cross-volume parents before canonical success. Exact nonce-bound pre-marker parent remnants may resume; foreign or preexisting empty state is never adopted. Public child-process admission tests plus guarded stale-handle commit tests cover state-directory and lifecycle-lock replacement on Windows and Linux. New sources and directory-created descendants are admitted to the write domain and may authorize when their complete current evidence has no adverse lifecycle delta.
- The lifecycle-store header now persists a nonzero `StoreGeneration`. Store-owned database and read/write transaction wrappers are lifetime-bound to the held namespace guard, so backend handles cannot cross its repository lock scope. Audit attempts and operation sessions retain only opaque generation tokens across analysis; every later physical or logical mutation reopens the canonical store at that generation and returns a typed hard-stop before mutation when replacement advanced it. The store now publishes a canonical durable migration intent, copies every known logical table into private same-volume source and target stores, validates catalog/reference closure and external run evidence, atomically replaces `lifecycle.store`, flushes the state parent, and idempotently removes intent and private payloads. Unknown tables, malformed references, orphan targets, missing canonical state, and generation disagreement hard-stop. Real child-process death at each intent/copy/replace/flush/cleanup boundary reopens to exactly the next authoritative generation; old attempt and gate-operation tokens cannot publish terminal state. This implements the current-schema copy-on-write generation replacement primitive; no historical schema adapter is claimed until a schema change exists.
- Run and gate retention now use immutable plan/content identities, independent pins, active-transition exclusions, repository-scoped pagination, and idempotent operation records. Confirmation advances `Prepared -> Pruning -> Pruned`, binds canonical tombstones before physical moves, keeps source-or-trash exactly one through recovery, and preserves the first committed operation result while later retries may finish current physical reclamation. Named debug-only crash points terminate real CLI child processes before plan/pruning commits, after each payload move and `Pruned` commit, and at every reclamation boundary; public target lookup, plan show, and operation show agree after each death before the same operation resumes. Immutable run evidence is opened through redb's read-only API so observation cannot mutate the hash-bound store bytes. The crash feature is rejected in release builds and has a dedicated Windows/Linux CI step. Public run/plan/lookup/operation DTOs survive process reopen and expose tombstones instead of plain not-found. The public latest-protection corpus separates a failed `latestAttempt` from its successful `latestCompleted` run, verifies both exclusion reasons and their linked attempt/run closure through plan show, then publishes a newer completion and proves confirmation returns `Stale` while the plan remains `Prepared` and both retained run evidence and latest pointers stay live.
- Run publication now owns durable attempt liveness, canonical `Running | Completed | Failed | Interrupted` envelopes, atomic run-directory publication, and independently monotonic latest-attempt/latest-completed fields. Nine named debug-only crash points cover pre-allocation reservation and lock creation, running publication, run rename, terminal attempt, and latest-pointer temp/replace boundaries through public CLI child processes. Recovery validates pending remnants before removal, removes only allocation-bound or self-bound pre-running liveness locks, never adopts a renamed orphan as success, finishes terminal publication idempotently, releases process-liveness artifacts, and keeps explicit-run overview separate from repository-latest attempt state. An attempt-only overview keeps the first failed attempt queryable before any completed run exists, and lifecycle migration revalidates every active lease's external lock identity and contents before copying the store. A debug-only loopback barrier now lets the public child-process corpus hold successful sequence 10 and 11 publishers after terminal attempt durability but before the exclusive guard, force reverse completion, and combine a newer failed attempt with an older successful run; the final public overview and catalog prove independent monotonic `latestAttempt` and `latestCompleted` fields without private-store mutation. Publication prepares immutable run/attempt payloads under an ordinary validated lock scope, then reacquires the exclusive catalog guard and rejects any validated run, attempt, or orphan tombstone before catalog or pointer publication. The public `publication-retention-race` corpus forces both guard orders: publication-first leaves confirmation `Stale`, while retention-first returns typed `RunRetentionState`, releases attempt liveness, and preserves the prior latest target without a dangling pointer. The crash feature is rejected in release builds and has dedicated Windows/Linux CI steps.

- The public `retention-plan-pagination` corpus now creates 51 completed runs, pages all 153 independently accounted plan records exactly once across the fixed 100-record boundary, and preserves the first plan ID, content identity, ordering, total, and cursor after an unrelated audit. Reusing that cursor against a second plan exits malformed with empty stdout, leaves the second plan `Prepared`, and never silently restarts or replaces either plan.

- The public `retention-independent-pins` corpus now creates two completed runs, derives the nonlatest attempt through `runs list`, allocates two distinct `PinId` values through `runs pin`, and proves plan show reports both sorted IDs on the linked Attempt and Run while keeping their Attempt/Run/Evidence closure out of prune items. Unpinning the first exact `PinId` leaves only the second protection; unpinning the final ID removes `active-pin` exclusions and makes the Attempt, Run, Evidence, and both inactive pin/reference records eligible together.

- The public `retention-active-transition-reference` corpus now opens disjoint gates A and B, closes B while A remains active, and derives B's exact terminal transition sequence through `gate show`. Public gate plan/show reports A as `active-gate`, excludes B exactly once as `active-transition-reference` with `gateIds` equal to A's exact gate ID, and exposes no prune items while that reference is live. After A closes by reconciling that exact sequence and clears its transition references, a new plan has no exclusions and exposes B's Gate, both GateRevisions, baseline and terminal Evidence, open and close Operations, and exact Transition capsule together.

- The public `lifecycle-operation-idempotency` corpus now drives pre-write, post-write, gate abandon, run pin/unpin, and both run/gate prune plan/confirm through child processes. Every injected post-commit stdout failure exits `1` without delivering the result; `operation show` reopens the committed result, an exact retry returns it again, conflicting same-ID reuse exits malformed, and gate revision history, active pin IDs, nonempty plan/tombstone lookups, and plan states prove that no second logical mutation occurred. Pin operation recovery also bounds the current Unix-millisecond JSON number against an independent wall clock before round-tripping it into the `u128` domain field. The debug-only delivery fault feature is rejected in release builds and has dedicated Windows/Linux CI lint and test steps.

- The public `gate-reopen-after-process-exit` corpus now opens and closes one gate through separate CLI child processes, reopens exact committed revision 1 through `gate show --revision`, and traverses 101 immutable findings across the fixed 100-record boundary. After an unrelated audit and a second gate transition, the original cursor resumes its exact gate/revision scope while cross-gate, cross-revision, and run-cursor reuse exits malformed. `gate explain` projects the selected finding's stable source/payload-bound evidence and bounded evidence/relations envelopes; owner canonicalization sorts and deduplicates identical nested rows before persistence and delta identity.

- The public `bounded-nested-query` corpus now drives real audit, pre-write, post-write, run findings/explain, and gate findings/explain child processes over 102 canonical findings, 102 evidence rows, and 101 grounded finding relations. Run and exact gate-revision scopes traverse `100+2`, `100+2`, and `100+1` pages exactly once under `findings.v1`, `evidence.v1`, and `relations.v1`; revision-0 cursors remain resumable after revision 1 exists, while run, gate, revision, finding, collection, repository, structured payload mutation, and nonboundary-anchor reuse exits malformed without page-one restart. Query policy and canonical continuation boundaries are owner-validated, protocol owns versioned content-bound `lumin-run-cursor.v2` and `lumin-gate-cursor.v2` codecs, and top-level `lumin explain --run` exposes the same bounded nested contract.

- The public `capabilities-pagination` corpus now exposes the four grounded compiled capabilities as fixed `3+1` pages under `capabilities.v1`. Current-binary traversal works without `.lumin`, binds content-bound `lumin-binary-cursor.v2` to a model-owned semantic `BuildIdentity`, and rejects cross-build or run cursors; exact-run traversal reuses repository-bound `lumin-run-cursor.v2`, survives later audits, and rejects cross-run or cross-repository reuse. Both scopes return each exact capability ID/state once, and duplicate registry or persisted-run IDs hard-stop instead of being silently deduplicated.

- The public `collection-ordering` corpus now binds all seven owner-defined collections to explicit total orders: findings, nested evidence, relations, file findings, runs, active gates, and retention-plan items. `lumin related --run`, `lumin files --run`, and `lumin gate list --active` expose the previously missing verticals with content-bound cursors, canonical continuation boundaries, collection envelopes, and typed malformed/stale/integrity exits. Run catalogs sort visible records by attempt sequence descending then run ID, while active gates sort opening transition sequence then gate ID and advance a transaction-local catalog revision exactly once for authorized open, close, or abandon membership changes. Duplicate semantic anchors, active-gate key/baseline/tombstone contradictions, and nonboundary cursors fail closed.

- Rust production and behavioral-corpus owners under `crates/` now stay below 1,000 lines. CLI query commands, inventory unit tests, bounded cursor-integrity scenarios, and semantic-demand write-gate scenarios live in focused owner modules; frozen historical probe snapshots under `reviews/` remain unchanged.

- The development-only `lumin-xtask architecture-check` command now hard-stops workspace-member and kind-specific local dependency drift, production dependencies on the tool, direct redb/OXC owner violations, direct production `std::process::Command` ownership, non-engine or malformed Rayon pool construction, named `ScanLock` types, global Rayon entry points, and byte drift in the three frozen semantic artifacts. Its output explicitly leaves path-owner, generated-table, limitation-registry, codec-runtime, third-party command re-export, corpus, package, and benchmark proof deferred rather than promoting partial coverage to Phase 1 completion.

- The development-only `lumin-xtask corpus foundation` orchestrator now binds an exact ordered 90-row registry to SLICE-001 Section 9, rejects registry/spec drift, isolates nested Cargo targets by mode and fault feature, and requires every successful mapped invocation to prove a real packaged `CARGO_BIN_EXE_lumin` child execution through an exact row marker. `None` remains not-applicable, `Some(&[])` remains required-but-unmapped, and zero-selection, malformed mappings, child/marker failures, or unmapped rows cannot report success. This is not yet a completed corpus or required CI gate: standard has 37 mapped and 49 unmapped of 86 applicable rows, determinism has 0 mapped and 86 unmapped, and store-crash has 4 mapped and 6 unmapped of 10 applicable rows. An actual standard run passed all 37 mapped rows with markers and exited `1` for the 49 unmapped rows; CI remains deliberately unwired until every applicable row passes. The public `source-role-findings-remain-visible` row proves that authored, generated-marker, explicitly vendored, and generated-marker-plus-authored-override files retain the same grounded finding shape in canonical and unfiltered totals; generated/vendor roles change only the exact `ReviewOnly` disposition, and an explicit file filter narrows results without changing `scopeTotal`. `source-role-classification` remains unmapped because the current public projection does not expose the required persisted rule version and configuration source. The public package-unsupported rows prove that `typesVersions` blocks silent use of an unspecialized `types` target and that nested-condition, array, and mixed-key `exports` shapes never select a fallback; each remains typed package-scoped incomplete while an unrelated root-package finding stays visible. The public `bundler-condition-excludes-node` row proves exact value/type identity selection across invocation-selected bundler, Node16, and NodeNext profiles: bundler never activates an earlier `node` branch and selects `default`, while Node16/NodeNext select `node`, with complete zero-limitation evidence and inverse exact finding sets.

- Audit and pre-write now accept repeated `--entry` together with the scan invocation tier. Invocation entries replace configured `lumin.json.entries`; normalized lexical duplicates collapse while physical aliases remain separate paths, and missing, ignored, excluded, out-of-domain, or hard-excluded entries persist typed incomplete evidence instead of disappearing. Caller-reserved or physically escaping entries fail before audit-attempt or gate-operation admission. The exact include/exclude/role/entry/profile tier, effective entry source/availability, root `lumin.json` presence or absence, and applicable root-to-leaf `.gitignore` payload identities participate in `AnalysisInputId` and gate semantic reads; post-write reconstructs the exact persisted opening tier and rejects corrupt projections or replacement flags. Explicit includes can re-admit gitignored sources, while entries alone cannot override exclusions. The public `scan-invocation-containment` child-process row now proves scan flags in the operation digest and `AnalysisInputId`, exact post-write tier reuse, replacement-flag rejection, caller/config escape separation, typed excluded-entry evidence, and a Windows junction retarget to an outside-root entry producing a durable active/incomplete close instead of authorization. The public `explicit-entry-selection` row uses the additive gate-baseline `entrySelections` projection to prove configured-entry replacement, identical-entry deduplication, hard-link aliases as separate logical contexts, exact missing/excluded limitations, independent public-package target protection, and the distinction between private-package export-level findings and forbidden package-unreachable absence. Focused owner, protocol, CLI, gate, Clippy, workspace-check, architecture, and actual marker-backed corpus checks pass.

- Public child-process rows now also prove plain ESM named/default/type-only fan-in versus side-effect reachability, a dead export sibling inside a reachable module, and identity-scoped public re-export protection. Six Vue/SFC rows prove direct Vue entry resolution, inline script-setup template binding, external-script attachment plus typed mode conflict, scoped missing-target evidence without graph abortion, non-source style assets without source probing, and the shared dialect boundary where Vue completes while Svelte/Astro stay explicit unavailable evidence. Declaration type imports resolve declaration facts while value imports cannot use them, declaration files never become removal candidates, and Next route-group parentheses remain ordinary path bytes. These twelve rows pass exact DTO assertions, marker validation, focused Clippy, and independent P0/P1/P2=0 audits. The `ignore-precedence` row additionally proves hard `node_modules` exclusion without basename heuristics for authored target/build/coverage directories, root-to-leaf `.gitignore` negation, explicit include over repository ignore, exclusion over inclusion, and non-`.gitignore` exclude lanes remaining unobserved. `extension-probe-precedence` remains deliberately unmapped because relative directory package/index probing is not implemented; typed unresolved evidence is not promoted to completion of that row.

Next implementation order: continue closing the remaining SLICE-001 acceptance, corpus, package/skill, and achieved-budget proofs before Phase 2 capability work.

## Routing Rules

- Starting any implementation, refactor, review, or specification change: read METHOD-000.
- Product identity, user-visible guarantees, supported environments: read PRODUCT-000.
- Adding, removing, or changing a crate dependency: read ARCH-000.
- Rayon, task ordering, cancellation, locks, memory, or determinism: read ARCH-001.
- Store, queries, SARIF, pre-write, post-write, or parallel agents: read ARCH-002.
- Any Phase 1 implementation or test: read SLICE-001.
- Architecture freeze or review resolution: read REVIEW-002, then follow any explicit predecessor link it cites.
- Repository working rules and close-out discipline: read `문서(한글)/AGENTS.ko.md` (canonical) or `AGENTS.md` (English translation).

## Phase Ledger

| Phase | State | Exit condition |
| --- | --- | --- |
| Phase 0: architecture | frozen | Independent document/design review, standalone feasibility, clean provenance, the unmuted finding amendment, and owner-approved numeric targets are complete. External target audit is advisory unless it supplies a concrete counterexample. |
| Phase 1: foundation slice | active | SLICE-001 passes its complete acceptance corpus on Windows and Linux prebuilt binaries. |
| Phase 2: capability growth | not started | New capabilities enter through the frozen DAG without creating a second engine. |
| Phase 3: legacy retirement | not started | Required compatibility exports and corpus parity are complete; Node analysis paths are removed. |
